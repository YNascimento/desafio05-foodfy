{% extends "admin/layout.njk" %}


{% block head %}
    <title>Foodfy - Admin</title>
    <link rel="stylesheet" href="/admin/chefs/list.css">
{% endblock %} 


{% block content %}   

    <div class="content-box">
        <div class="header-box">
            <h2>Gerenciar Chefs</h2>
            <div>
                <a href="/admin/chefs/create" class="adminBtn" type="submit">Criar Chef</a>
            </div>
        </div>
        

        <div class="cards">
            {% for chef in chefs %}
            <div class="card">
                <img src="{{chef.avatar_url}}" alt="">
                <div>
                    <h4>{{chef.name}}</h4>
                    <a href="/admin/chefs/{{chef.id}}">Visualizar</a>
                </div>
            </div>
            {% endfor %}
        </div>
    </div>
{% endblock %} 
